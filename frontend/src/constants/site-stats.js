/**
 * Site-wide statistics constants
 * Single source of truth — import these everywhere instead of hardcoding.
 *
 * TODO: Khi có dữ liệu thật từ backend, thay bằng API call và cập nhật các giá trị này.
 * Các con số dưới đây là ước tính thực tế của trung tâm tại thời điểm khai trương website.
 */

// Tổng học viên đã đăng ký (cộng dồn, bao gồm cựu học viên)
export const TOTAL_STUDENTS = '500+';

// Học viên đang học hiện tại
export const ACTIVE_STUDENTS = '100+';

// Số năm kinh nghiệm hoạt động
export const YEARS_EXPERIENCE = '10+';

// Số chương trình/khóa học
export const TOTAL_COURSES = '08';

// Tỷ lệ hài lòng (%)
export const SATISFACTION_RATE = '95%';

// Số giảng viên cơ hữu và thỉnh giảng
export const TEACHER_COUNT = '10+';

// Điểm IELTS tối thiểu của đội ngũ giảng viên
export const TEACHERS_MIN_IELTS = '8.0+';
